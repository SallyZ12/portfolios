<h5> Transaction Show Page </h5>


<table class = "table">
<thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

<tbody>

  <tr>

    <td> Transaction </td>
    <td> Series </td>
    <td> Par </td>
    <td> Trans ID </td>

  </tr>

    <td> <%= @transaction.name %> </td>
    <td> <%= @transaction.series %> </td>
    <td> <%= @transaction.par %> </td>
    <td> <%= @transaction.id %> </td>


  </tr>

  </tr>
  </tbody>
  </table>
  <br><br><br>

<%= link_to "Edit Transaction", edit_exposure_transaction_path(@transaction.exposure_id, @transaction) %>
<br><br>
<%= form_tag exposure_transaction_path(@transaction.exposure.id, @transaction.id), method: "delete" %>
<%= submit_tag "Delete #{@transaction.name}"  %><br><br>
